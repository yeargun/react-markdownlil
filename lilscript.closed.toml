# closed LilScript world: no builtin or callback assumptions (the `./closed` export)
[javascript]
priority = "size-first"
optimization_level = 13
cost_model = "brotli"
assume_pristine_builtins = false
strip_console = true
candidate_search = "production"

[mangle]
identifiers = true
properties = true
pool_strings = true
